import { View, Text } from "react-native";
import React from "react";

const student = () => {
  return (
    <View className=" flex-1 justify-center items-center">
      <Text className="text-lg font-semibold ">Student Details</Text>
    </View>
  );
};

export default student;
