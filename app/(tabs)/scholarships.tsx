import { View, Text } from "react-native";
import React from "react";

const scholarships = () => {
  return (
    <View className=" flex-1 justify-center items-center">
      <Text className="text-lg font-semibold">Scholarship Transaction</Text>
    </View>
  );
};

export default scholarships;
