import { View, Text } from "react-native";
import React from "react";

const school = () => {
  return (
    <View className=" flex-1 justify-center items-center">
      <Text className="text-lg font-semibold">School Reports Card</Text>
    </View>
  );
};

export default school;
